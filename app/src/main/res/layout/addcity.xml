<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/chooseCity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:text="הוסף יישוב לימוד   (ניתן להוסיף עד 4 יישובים)"
        android:textColor="@color/white" />

    <ListView
        android:id="@+id/listViewCity"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_marginTop="20dp"
        android:layout_below="@+id/ButtonCityDialog" >

    </ListView>
    


    <Button
        android:id="@+id/ButtonCityDialog"
        style="@style/Base.Widget.AppCompat.ActionButton"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/chooseCity"
        android:text="הוסף יישוב"
        android:background="@drawable/button_border"
        android:textColor="@color/white" />

    <Button
        android:id="@+id/buttonSubmitCity"
        style="@style/Base.Widget.AppCompat.ActionButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/listViewCity"
        android:layout_centerHorizontal="true"
        android:text="אשר"
        android:textColor="@color/white" />

</RelativeLayout>